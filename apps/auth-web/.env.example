# auth-web 환경변수 예시. 실제 파일은 .env.local (gitignore 됨).
# dev: 로컬 auth-api 직접 호출
# prod: 같은 origin (auth.get-it.cloud/api) — VITE_API_URL 비워두면 axios baseURL 기본값 '/api' 사용
VITE_API_URL=http://localhost:3001/api
